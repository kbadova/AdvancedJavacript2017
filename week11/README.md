# A typescript project that uses socket io and connects BE to FE

## Steps to run the project:
* npm install
* tsc -> compiles the typescript files to pure js
* node ./dist/server.js
* go to localhost:3000/ and see the console
